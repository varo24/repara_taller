
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite lectura y escritura si el usuario está autenticado (incluyendo anónimos)
      allow read, write: if request.auth != null;
    }
  }
}
